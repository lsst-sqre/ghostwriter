# -*- conf -*-
#
# Editable runtime dependencies (equivalent to install_requires)
# Add direct runtime dependencies here, as well as implicit dependencies
# with constrained versions.
#
# After editing, update requirements/main.txt by running:
#     make update-deps
# (make-update-deps-no-hashes while we're using an unreleased RSP client)

# These dependencies are for fastapi including some optional features.
fastapi
starlette
uvicorn[standard]

# Other dependencies.
pydantic
pydantic-settings
safir>=5

PyYAML

rubin.nublado.client@git+https://github.com/lsst-sqre/nublado#subdirectory=client
httpx_sse  # Apparently not discovered from rubin.nublado.client
